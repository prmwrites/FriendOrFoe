<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @relation.name %>
</p>

<p>
  <strong>Parent:</strong>
  <%= @relation.parent_id %>
</p>

<%= link_to 'Edit', edit_country_relation_path(@country, @relation) %> |
<%= link_to 'Back', conutry_relation_path(@country) %>
